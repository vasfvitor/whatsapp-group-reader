# Removido do pacote distribuído (staging) antes de zipar.
# Duas formas de entrada:
#   - Caminho literal relativo à raiz do staging (arquivo ou pasta).
#   - Padrão recursivo "**/<filtro>" aplicado sob app/node_modules;
#     terminar com "/" limita o filtro a diretórios.
# NÃO adicionar: puppeteer-core/lib/* (alvos vivos de "exports") nem
# fluent-ffmpeg (require ansioso do Util do whatsapp-web.js).

# Documentação e metadados sem uso em runtime
**/*.md
**/*.markdown
**/.github/
**/test/
**/tests/
